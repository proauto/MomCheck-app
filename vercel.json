{
  "installCommand": "npm install --prefix momcheck-app",
  "buildCommand": "npm run build --prefix momcheck-app",
  "outputDirectory": "momcheck-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}