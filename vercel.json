{
  "buildCommand": "cd momcheck-app && npm install && npm run build",
  "outputDirectory": "momcheck-app/dist",
  "installCommand": "echo 'Skip root install'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}